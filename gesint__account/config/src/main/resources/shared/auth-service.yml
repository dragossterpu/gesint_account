spring:
  data:
    mongodb:
      host: auth-mongodb
      username: user
      password: ${MONGODB_PASSWORD}
      database: gesint_account
      port: 27017

server:
  servlet:
    context-path: /uaa
  port: 5000
